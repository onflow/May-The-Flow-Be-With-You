name: Daily Sync Leaderboard

on:
  pull_request:
    types: [closed]
    paths:
      - 'submissions/**'

jobs:
  sync-and-submit:
    runs-on: ubuntu-latest
    env:
      NETWORK: ${{ secrets.NETWORK }}
      MAINNET_FLOW_ADDRESS: ${{ secrets.MAINNET_FLOW_ADDRESS }}
      MAINNET_FLOW_PRIVATE_KEY: ${{ secrets.MAINNET_FLOW_PRIVATE_KEY }}
      TESTNET_FLOW_ADDRESS: ${{ secrets.TESTNET_FLOW_ADDRESS }}
      TESTNET_FLOW_PRIVATE_KEY: ${{ secrets.TESTNET_FLOW_PRIVATE_KEY }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: |
          bun install

      - name: Run syncLeaderboardPeriod
        run: |
          bun run on-chain-leaderboard/src/commands/scan-and-submit-topics.ts
